[pytest]
testpaths = tests
python_files = test_*.py
markers =
    api: 実際の画像生成APIを呼ぶテスト（課金されます）。通常は実行しません。
# 既定では api マーカー付きのテストを除外します。実行したい場合: pytest -m api
addopts = -q --strict-markers -m "not api"
